<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="study.dao.TestDao">
  
  <insert id="insert1" parameterType="Test01">
    INSERT INTO test01(title)
    values(#{title})
  </insert>
  
  <insert id="insert2" parameterType="Test02">
    INSERT INTO test02(title)
    values(#{title})
  </insert>
</mapper>

















